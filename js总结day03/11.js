var name = '张三';  // 定义变量name为张三
    var age ; // 未定义
    const gender ="女"; // 定义常量
    name = "李四"；// 改变变量
    console.log(name);// 输出name
    console.log(age); // 输出age
    gender = "男";// 改变常量
    console.log(gender);//报错